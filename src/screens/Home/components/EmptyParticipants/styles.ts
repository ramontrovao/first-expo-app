import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  emptyContainer: {
    marginTop: 82,

    justifyContent: "center",
    alignItems: "center",
    gap: 16,
  },
  emptyText: {
    textAlign: "center",
    color: "#808080",
    fontSize: 16,
  },
});
